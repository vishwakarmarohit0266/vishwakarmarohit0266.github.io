    :root{
      --bg:#0f1724; /* deep navy */
      --card:#111827;
      --accent1:#7c3aed;
      --accent2:#06b6d4;
      --muted:#94a3b8;
      --glass: rgba(255,255,255,0.04);
      --radius:16px;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }
    *{box-sizing:border-box}
    html,body{height:100%;margin:0;background:linear-gradient(180deg,var(--bg) 0%, #071029 100%);color:#e6eef8}
    .wrap{min-height:100vh;display:flex;align-items:center;justify-content:center;padding:48px}

    .card{
      width:100%;max-width:1000px;display:grid;grid-template-columns:1fr 360px;gap:32px;padding:36px;background:linear-gradient(135deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));border-radius:20px;box-shadow:0 10px 30px rgba(2,6,23,0.6);
    }

    /* left: hero content */
    .hero h1{margin:0;font-size:clamp(28px,4vw,42px);line-height:1.02}
    .handle{color:var(--muted);font-size:14px;margin-bottom:10px}
    .hero p{color:var(--muted);margin:18px 0 22px;max-width:52ch}

    .tagline{font-weight:600;margin-bottom:18px;}

    .badges{display:flex;gap:10px;flex-wrap:wrap}
    .badge{background:var(--glass);padding:8px 12px;border-radius:999px;font-size:13px;color:var(--muted);backdrop-filter: blur(6px);}

    .cta-row{display:flex;gap:12px;margin-top:18px}
    .btn{padding:12px 16px;border-radius:12px;border:0;cursor:pointer;font-weight:600}
    .btn-primary{background:linear-gradient(90deg,var(--accent1),var(--accent2));color:white;box-shadow:0 8px 20px rgba(92,40,220,0.18)}
    .btn-ghost{background:transparent;border:1px solid rgba(255,255,255,0.06);color:var(--muted)}

    /* right: preview / card */
    .preview{background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));padding:18px;border-radius:14px;display:flex;flex-direction:column;gap:12px}
    .preview .title{font-size:13px;color:var(--muted)}
    .mock{height:220px;border-radius:12px;background:linear-gradient(135deg, rgba(124,58,237,0.12), rgba(6,182,212,0.06));display:flex;flex-direction:column;justify-content:center;align-items:flex-start;padding:18px}
    .mock h3{margin:0 0 6px}
    .mock p{margin:0;color:var(--muted);font-size:13px}

    footer.small{margin-top:8px;font-size:12px;color:var(--muted)}

    /* typing effect */
    .skills{font-weight:700}
    .cursor{display:inline-block;width:10px;height:22px;background:var(--accent1);margin-left:6px;vertical-align:middle;animation:blink 1s steps(2) infinite}
    @keyframes blink{50%{opacity:0}}

    /* responsive */
    @media (max-width:880px){
      .card{grid-template-columns:1fr;}
      .mock{height:160px}
    }
